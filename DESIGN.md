# DCC Chess — Design Reference

Generated by reading `dcc_chess/pawns.py` and `app.py` (plus verified knowledge of
`dcc_chess/abilities.py`'s live implementations from the session that produced this
document — no game code was changed to produce this file). Where the two sources
disagree, this document says so explicitly and states which one is authoritative
(the code, always — `pawns.py`/`app.py` descriptions are player-facing flavor text
and can drift out of sync with the implementation).

**Trigger legend.** The code's `AbilityTrigger` enum (`dcc_chess/pawns.py`) has four
values: `FLOOR_ROLL`, `AUTO_ON_CAPTURE`, `AUTO_DEFENSE` (declared but unused by any
of the 20 pawns), and `NO_ROLL`. This document maps them to the three categories
requested for this reference:
- **FLOOR_ROLL** → a die is spent; the roll must meet or beat the floor number.
- **AUTO** → fires automatically with no player action (`AUTO_ON_CAPTURE`), or is
  simply always-on/passive (no trigger at all).
- **REACTION** → `NO_ROLL` pawns whose actual behavior is a reactive defense
  (Quasar). Other `NO_ROLL` pawns are marked N/A (no active ability) or noted
  individually (Juice Box, Garret, Orthrus).

---

## Section 1 — All 20 Pawns

"The AI" exists in `PAWN_CHARACTERS` (`dcc_chess/pawns.py`) but is explicitly excluded
from `/roster` in `app.py` ("excluded here so it can never be drafted, auto-drafted,
or placed in any game mode") — it is not one of the 20 and is not documented below
beyond this note.

| Pawn | Ability | Cost | Trigger | Uses/Game | Combined? |
|---|---|---|---|---|---|
| Zev | Biggest Fan | 3 | FLOOR_ROLL | Unlimited | No |
| Mordecai | Manager Benefit | — | AUTO | Unlimited (every capture) | No |
| Prepotente | Special Boy | 4 | FLOOR_ROLL | Unlimited | No |
| Elle McGib | Frozen | 5 | FLOOR_ROLL | Unlimited | No |
| Imani | Suppress | 4 | FLOOR_ROLL | Unlimited | No |
| Slugalo | One Of Us | 10 | FLOOR_ROLL | 1 | Yes |
| Louie | Air Strike | 6 | FLOOR_ROLL | Unlimited | Yes |
| Sledge | Body Guard | 4 | FLOOR_ROLL | Unlimited | No |
| Stripper Anaconda | Gun Show | 5 | FLOOR_ROLL | Unlimited | No |
| Quasar | Mediation | 6 (banked die) | REACTION | 2 | No |
| Lucia Mar | Sic Em | 3 | FLOOR_ROLL | Unlimited | No |
| Chris | Lava Surge | 4 | FLOOR_ROLL | Unlimited | No |
| Juice Box | Shapeshift | 0 / meta | NO_ROLL (meta) | N/A | Matches copied ability |
| Florin | Suppressing Fire | 6 | FLOOR_ROLL | Unlimited | No |
| Garret | Indestructible | — | AUTO (passive) | N/A | No |
| Signet | Succubus | 6 | FLOOR_ROLL | Unlimited | No |
| Miriam Dom | Blood Magic | 8 | FLOOR_ROLL | Unlimited | Yes |
| Orthrus | No Ability | — | N/A | N/A | No |
| Raul the Crab | Group Climax | 7 | FLOOR_ROLL | Unlimited | Yes |
| Bad Llama | Lava Spit | 4 | FLOOR_ROLL | Unlimited | No |

### Per-pawn detail

**Zev — Biggest Fan.** Sets `zev_buff_active[color] = True` on success; consumed at
the start of that color's next turn to add +1 to every friendly die roll for that
turn only. Matches `pawns.py` exactly.

**Mordecai — Manager Benefit.** No manual trigger; fires when he's captured
(`process_mordecai_capture`): creates a single-square ghost token on his capture
square for 3 full turns (blocks entry) and schedules his respawn on his own back
rank after those 3 turns. Also grants a passive aura: while within 1 square of
Carl or Donut, their ability costs are reduced by 1. Matches `pawns.py`. Note: in
the Juice Box dispatcher (`try_juice_box_use_captured_ability`), the `if/elif` name
chain has no branch for "Mordecai" at all — if Juice Box ever acquired his name
(pre-v0.60 filter), clicking it silently failed rather than erroring. As of the
v0.60 FLOOR_ROLL filter, "Mordecai" (`AUTO_ON_CAPTURE`) can no longer be acquired by
Juice Box in the first place, so this dead branch is now unreachable — moot but left
as-is in the code.

**Prepotente — Special Boy.** Moves 2 squares forward (including 2-square diagonal
captures) normally. **Mismatch:** `pawns.py` says "if this move specifically saves
Carl from check" he can move 2 squares in any direction — the code only checks
whether Carl is in check *at the moment the ability is cast* (`is_in_check`), not
whether the resulting move actually resolves that check. The code is what's live;
the "specifically saves" qualifier is not enforced. Also: the destination is always
chosen randomly by the server (`random.choice(result)`) — no UI path exists to pick
a specific destination for this ability, for either Prepotente or Juice Box hosting
it, so this is consistent (if under-featured) rather than a live bug.

**Elle McGib — Frozen.** Freezes one enemy piece within 5 squares for 1 full turn
(frozen piece can't move or use abilities). Matches `pawns.py` exactly. See
Section 3 — a second ability ("Frozen Immunity") existed earlier and was removed;
Elle McGib has exactly one ability now, matching the code.

**Imani — Suppress.** One enemy piece within 2 squares loses ability use for its
next turn. Matches `pawns.py`. Target is always chosen randomly — no manual
targeting UI exists for this ability.

**Slugalo — One Of Us.** Converts one enemy pawn to the caster's side permanently.
**Mismatches:** (1) `pawns.py`'s text states no range restriction at all ("any one
enemy pawn"), but the shared implementation (used whenever Juice Box hosts it, and
whenever a human plays Slugalo with no explicit target) restricts targets to within
2 squares — a number that appears nowhere in the `pawns.py` text. (2) Worse, `app.py`'s
inline handler for a human explicitly picking a target has *no* range check at all,
so Slugalo's own targeted use and the shared/Juice-Box path currently enforce two
different rules for the same ability. (3) "If captured it returns to original owner"
is not implemented anywhere — `GameState.recruited_pawns` is declared but never
written to or read.

**Louie — Air Strike.** Creates a 2×2 zone within 4 squares (all 4 squares must be
completely empty), blocked for 2 full turns. **Mismatch:** `pawns.py` says "Louie
cannot move this turn," but the code sets `louie_cant_move` to the *zone's own
squares*, not Louie's square — and since the zone must be empty to place, Louie is
essentially never standing in it. He is not actually prevented from moving on the
turn he casts this. The code is what's live; the "cannot move" line in `pawns.py`
is currently not enforced.

**Sledge — Body Guard.** Immovable and invulnerable (via `iron_wall_pieces`) for 2
full turns. Matches `pawns.py` exactly.

**Stripper Anaconda — Gun Show.** **Mismatch, code is authoritative:** `pawns.py`
describes "pull any one female piece 1 square closer to Anaconda." The implemented
ability does something entirely different: it gives all friendly *male* pieces +2
to dice rolls for 2 turns (`gun_show_active[color] = 2`). No "pull" mechanic exists
in the code for this ability at all.

**Quasar — Mediation.** See Section 3 for the full decision record. Fires
automatically (`attempt_capture`) as a defensive reaction whenever a friendly
non-Carl piece is about to be captured, up to twice per game, cost 6 (spent as a
single die, not combined). Matches `pawns.py` exactly.

**Lucia Mar — Sic Em.** Restrains one enemy piece anywhere on the board (no range
limit) for 1 full turn. Matches `pawns.py`. Target is always random.

**Chris — Lava Surge.** Covers Chris's square plus one square on each side, in a
chosen horizontal or vertical direction (3 squares total), with lava for 2 full
turns. Cannot cast while an enemy is adjacent; all 3 squares must be completely
empty; Chris cannot move for those 2 turns. Matches `pawns.py` exactly.

**Juice Box — Shapeshift.** Has no active ability of her own. Capturing an enemy
pawn adds that pawn's name to her acquired list — **as of the v0.60 fix, only if
that pawn's own trigger is `FLOOR_ROLL`** (`process_juice_box_capture`); acquiring
an `AUTO`/`REACTION`/passive pawn's name is now blocked at the point of capture, and
`get_piece_abilities` (`app.py`) and `appendJuiceBoxCard` (`static/game.js`) apply
the same filter as a second layer. She can fire an acquired ability from her own
square at the source pawn's base cost + 1, at most once per side per turn cycle
(`juice_box_cooldown`), and never the same turn she captured a pawn
(`juice_box_used_this_turn`). If the opponent resurrects the source pawn, she loses
that acquired ability (`_juice_box_lose_ability`). Matches `pawns.py`.

**Florin — Suppressing Fire.** Pushes one enemy piece up to 2 squares directly away
from Florin, stopping early if blocked. Matches `pawns.py`. Target is always random.

**Garret — Indestructible.** Passive, no manual trigger. Cannot be captured by a
normal capture; can only be removed by the enemy Carl moving onto his square, or
by Miriam Dom's Blood Magic. Cannot capture enemy pieces himself. Matches `pawns.py`
as far as this document's sources (`app.py` has no Garret-specific logic at all —
this is entirely implemented in `dcc_chess/abilities.py`, not re-verified against
this document's two source files).

**Signet — Succubus.** **Mismatch, code is authoritative:** `pawns.py` describes
"draw any 1 male character 1 square closer to Signet." The implemented ability
instead prevents every enemy male piece within 3 squares from moving on its next
turn (`succubus_pending`). No "pull" mechanic exists in the code for this ability.

**Miriam Dom — Blood Magic.** Sacrifices an adjacent friendly pawn (does not trigger
on-capture effects; Garret can be sacrificed) to resurrect a previously-captured
friendly pawn onto an open back-rank square. Matches `pawns.py`. Note: same pattern
as Slugalo — `app.py`'s inline handler for a human explicitly picking a sacrifice
target does not re-check adjacency the way the shared implementation does; not
reachable through normal play today since `get_ability_targets` already restricts
the clickable squares to adjacent ones, but the two code paths disagree in principle.

**Orthrus — No Ability.** No active ability. 1×2 piece; moves 1 square in his
facing direction or rotates 90° (one action per turn); cannot capture (a move into
an occupied enemy square is illegal for him); can only be captured by a major piece
landing on his head or butt square; that capture is permanent (excluded from every
resurrection path in the game, including Juice Box — she can never capture him at
all, since only major pieces are allowed to). Matches `pawns.py` exactly.

**Raul the Crab — Group Climax.** Sets a pending flag for the caster's side; at the
start of that side's next turn the flag is consumed and every friendly ability's
floor cost drops by 2 for that turn only (`promote_group_climax`, called from
`/new_game`, `/start_turn`, and the AI's own turn handler in `app.py`). Matches
`pawns.py`'s "-2 for the next full turn." See Section 4 — flagged there as a pending
item to re-verify end-to-end (e.g. reaction-based die spends that don't pass through
those three call sites).

**Bad Llama — Lava Spit.** As of the fix recorded in Section 3: places a 1×2
horizontal lava strip anywhere within 4 squares (extends right, or left at the
board edge) for 3 full turns; no piece can enter it while active; the caster cannot
move on the turn they cast it. Matches `pawns.py`'s current text. See Section 4 —
the "must move out or be captured" consequence for a piece already standing in the
zone when it's placed is not yet enforced by any code.

---

## Section 2 — All 5 Major Pieces

Source: `MAJOR_ABILITIES` in `app.py`. Each major has exactly 3 abilities, the third
always Boss Event Only. `app.py`'s `use_ability()` route also still contains dead
routing for a handful of older ability names (Carl's Bulldozer; Donut's Diva's
Entrance and Resurrection; Mongo's Rampaging Charge; Katia's Combat Roll and Dual
Threat; Samantha's The Mouth and Portal Spike) — none of these appear in
`MAJOR_ABILITIES`, so `get_piece_abilities` never surfaces them in the sidebar and
the frontend never sends them; they are unreachable through normal play and not
documented as live abilities below.

| Piece | Ability | Cost | Uses/Game | Combined? | Boss Only? |
|---|---|---|---|---|---|
| Carl | Leader | 0 (combined total) | 2 | Yes | No |
| Carl | Plot Armor | 8 | 1 | Yes | No |
| Carl | Jug-o-Boom | 4 | Unlimited | No | Yes |
| Donut | Puddle Jump | 7 | Unlimited (10-turn cooldown) | Yes | No |
| Donut | Cockroach | 7 | 1 | Yes | No |
| Donut | Magic Missile | 5 | Unlimited | No | Yes |
| Mongo | Pet Carrier | 4 | Unlimited (1 stored at a time) | No | No |
| Mongo | Rampage | 8 | 1 | Yes | No |
| Mongo | Gorefest | 4 | Unlimited | No | Yes |
| Katia | She Tank | 6 | 2 | No | No |
| Katia | Blitzed | 5 | Unlimited | No | No |
| Katia | I Need My Space | 3 | Unlimited | No | Yes |
| Samantha | Slut Shame | 8 | 1 | Yes | No |
| Samantha | Miss Me? | 5 | Unlimited | No | No |
| Samantha | IWKYM | 6 | Unlimited | No | Yes |

### Per-piece detail

**Carl**
- *Leader* — combine all available dice this turn (both rolled dice, or a banked
  die plus the rolled die) into a pull distance; choose one friendly back-line
  major (Donut, Katia, or Samantha) and pull it toward Carl by up to that many
  squares along its own normal movement path; Carl still makes his normal move.
- *Plot Armor* — moves 1–3 squares in any King direction; cannot pass through
  occupied squares but can capture the piece he lands on (the enemy King fully
  blocks, like a friendly piece — never a valid landing square); destination must
  not leave Carl in check.
- *Jug-o-Boom (Boss Event Only)* — throws a bomb up to 3 squares in any direction;
  hits if a boss square is the target or adjacent to it.

**Donut**
- *Puddle Jump* — hops unlimited squares in any Queen direction, passing harmlessly
  over every piece in the path; destination must be a completely empty square, so
  it can never capture. 10-turn cooldown after each use (see Section 3).
- *Cockroach* — resurrects one randomly-chosen captured friendly piece onto any
  open square adjacent to Donut.
- *Magic Missile (Boss Event Only)* — fires in a straight line up to 5 squares;
  hits if any boss square lies on the path.

**Mongo**
- *Pet Carrier* — removes Mongo from the board (stored); can be released for free
  on a later turn within 2 squares of Donut; if Donut is captured while he's
  stored, Mongo is captured too; only one Mongo can be stored at a time.
- *Rampage* — captures every enemy piece found along any of his knight-shaped
  movement paths, not just the final landing square. The enemy King is explicitly
  excluded as a valid target/capture (fixed in v0.59, mirroring Plot Armor's own
  King exclusion).
- *Gorefest (Boss Event Only)* — attacks 2 squares from Mongo's current position
  in any direction to damage a summoned boss.

**Katia**
- *She Tank* — prevents one enemy piece from moving on its next turn; usable as a
  reaction (spending a banked die) before the enemy moves, or normally at the
  start of Katia's own turn.
- *Blitzed* — one chosen friendly piece skips its movement requirement for the
  turn; that piece can still use its own ability.
- *I Need My Space (Boss Event Only)* — pushes the boss 2 squares directly away
  from Katia; any piece the boss is shoved into is permanently killed.

**Samantha**
- *Slut Shame* — swallows one enemy pawn within 3 squares (Orthrus is immune — a
  2-square body can't be swallowed), removing it from the board for 5 turns before
  it respawns on an open square adjacent to Samantha.
- *Miss Me?* — forces a reroll of any dice currently in play; usable at the start
  of Samantha's own turn on her own dice, or as a reaction (banked die) to counter
  an enemy roll.
- *IWKYM (Boss Event Only)* — while adjacent to the boss, holds it in place for 2
  full turns (it can still take damage); Samantha then releases and respawns on
  her own back rank.

---

## Section 3 — Key Design Decisions

These are settled decisions. Future sessions should treat them as given, not as
open questions to re-litigate.

- **Puddle Jump (Donut).** Repositioning only — it can never capture. Destination
  must be a completely empty square. Unlimited range in any Queen direction,
  passing harmlessly over every piece in the path (friendly or enemy — none of
  them are affected). Cost 7, requires combined dice. 10-turn cooldown after each
  use, tracked in `GameState.puddle_jump_cooldown` and ticked down once per
  `end_turn()` call. (v0.59.)

- **Quasar Mediation.** Cannot defend Carl himself, ever. Cannot be declared while
  the defending side's Carl is in check. Cannot save a piece that is itself
  currently delivering check to the enemy Carl (that would just hand the attacker
  Carl on the next move). Both sides roll 2d6 and add; the defender needs to win
  by 2 or more to save the threatened piece — a tie or a 1-point defender lead
  goes to the attacker (capture proceeds normally). Cost 6, twice per game.

- **Elle McGib.** The earlier "Frozen Immunity" ability has been removed. She has
  exactly one ability now: Frozen, cost 5.

- **Orthrus.** No capture ability of his own — a move into an occupied enemy
  square is illegal for him, full stop. Only major pieces (Carl, Donut, Mongo,
  Katia, Samantha) can capture him, by landing on either his head or butt square.
  His death is permanent — he is excluded from every resurrection/respawn path in
  the game (Cockroach, Blood Magic, Juice Box's captured-ability tracking never
  applies to him since pawns can't capture him in the first place).

- **Pawn abilities toggle (`pawns_enabled`, set from `/new_game`).** A single
  global flag that affects both the human and the AI identically, including
  every auto-trigger (Garret's Indestructible, Orthrus's capture restriction,
  Mordecai's Manager Benefit, etc.) — when off, none of it applies for either
  side, not just the manually-triggered FLOOR_ROLL abilities.

- **Bad Llama (Lava Spit).** 1×2 horizontal lava strip (not 2×2), cost 4, single
  die, lasts 3 full turns, no piece can enter while active, and the caster cannot
  move on the turn they cast it (`bad_llama_cant_move`, cleared at `end_turn()`,
  same pattern as `louie_cant_move`). (v0.60.)

---

## Section 4 — Known Pending Items

- **Bad Llama's zone force-movement mechanic is not enforced.** `pawns.py` and
  this document both describe "any piece currently in the zone when placed must
  move out on their next turn or be captured" — no code anywhere tracks which
  pieces were in the zone at placement or punishes them for staying. This is new
  state machinery, not a rewire of something that already exists.

- **Juice Box's full ability execution is not complete for all ability types.**
  The FLOOR_ROLL filter (v0.60) stops the worst symptom (dead cards that burn a
  turn for nothing), but coverage of every acquired ability's execution path
  hasn't been independently re-verified end to end since that fix.

- **Raul the Crab's Group Climax needs its `floor_modifier` wiring re-verified.**
  `promote_group_climax` is called from `/new_game`, `/start_turn`, and the AI's
  own turn handler, which covers the normal roll-a-fresh-turn flow — but this
  hasn't been confirmed against every path that can spend a die this turn (banked
  die pulls, reactions, etc.), so treat it as unverified rather than confirmed
  broken or confirmed working.

- **Multiplayer infrastructure is not yet built.** The server holds a single
  in-memory game (`game_data` is one global dict in `app.py`) — no concept of
  separate game rooms, sessions, or concurrent games exists yet.
